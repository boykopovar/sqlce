name: Build and release

on:
  push:
    tags:
      - "v*"
jobs:
  test:
    name: Run tests
    uses: ./.github/workflows/test.yml

  build-linux:
    name: Build wheel (Linux)
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.21.3
        env:
          CIBW_ARCHS_LINUX: x86_64
          CIBW_BUILD_VERBOSITY: 1

      - name: Build sdist
        run: |
          python -m pip install --upgrade pip build
          python -m build --sdist

      - name: Collect wheels and sdist into one flat folder
        run: |
          mkdir -p collected
          cp wheelhouse/*.whl collected/
          cp dist/*.tar.gz collected/

      - uses: actions/upload-artifact@v4
        with:
          name: dist-linux
          path: collected/*

  build-windows:
    name: Build wheel (Windows)
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.21.3
        env:
          CIBW_ARCHS_WINDOWS: AMD64
          CIBW_BUILD_VERBOSITY: 1
          CIBW_CMAKE_ARGS: "-DSQLCE_BUILD_PYTHON_BINDINGS=ON"

      - uses: actions/upload-artifact@v4
        with:
          name: dist-windows
          path: wheelhouse/*

  build-cpp-linux:
    name: Build C++ library (Linux, ${{ matrix.link }})
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        link: [static, shared]
    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHARED_LIBS=${{ matrix.link == 'shared' && 'ON' || 'OFF' }}

      - name: Build libsqlce
        run: cmake --build build --target libsqlce -j

      - name: Package library + headers
        run: |
          version="${GITHUB_REF_NAME#v}"
          pkg="libsqlce-${version}-linux-x86_64-${{ matrix.link }}"
          mkdir -p "$pkg/lib" "$pkg/include"
          cp build/dist/lib/libsqlce.* "$pkg/lib/"
          cp -r core/include/. "$pkg/include/"
          tar -czf "${pkg}.tar.gz" "$pkg"

      - uses: actions/upload-artifact@v4
        with:
          name: cpp-linux-${{ matrix.link }}
          path: libsqlce-*.tar.gz

  build-cpp-windows:
    name: Build C++ library (Windows, ${{ matrix.link }})
    needs: test
    runs-on: windows-latest
    strategy:
      matrix:
        link: [static, shared]
    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        shell: pwsh
        run: |
          $shared = if ("${{ matrix.link }}" -eq "shared") { "ON" } else { "OFF" }
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=$shared

      - name: Build libsqlce
        run: cmake --build build --target libsqlce --config Release -j

      - name: Package library + headers
        shell: pwsh
        run: |
          $version = "${env:GITHUB_REF_NAME}" -replace '^v', ''
          $pkg = "libsqlce-$version-windows-x86_64-${{ matrix.link }}"
          New-Item -ItemType Directory -Force -Path "$pkg/lib" | Out-Null
          New-Item -ItemType Directory -Force -Path "$pkg/include" | Out-Null
          Copy-Item "build/dist/lib/libsqlce.*" "$pkg/lib/"
          Copy-Item -Recurse "core/include/*" "$pkg/include/"
          Compress-Archive -Path $pkg -DestinationPath "$pkg.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: cpp-windows-${{ matrix.link }}
          path: libsqlce-*.zip

  pages:
    name: Pages Wasm Demo
    needs: test
    uses: ./.github/workflows/pages.yml
    permissions:
      contents: read
      pages: write
      id-token: write

  publish:
    name: Publish to PyPI
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          path: dist
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist

  publish-release:
    name: Publish GitHub Release
    needs: [build-cpp-linux, build-cpp-windows, build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: cpp-*
          path: release-assets
          merge-multiple: true

      - uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          path: release-assets
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
